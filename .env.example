# Copy this file to `.env` and fill in your own keys.
# `.env` is gitignored and never deployed.

# Free key from https://console.groq.com/keys
GROQ_API_KEY=

# Optional alternatives — only one needs to be set
# GEMINI_API_KEY=
# OPENROUTER_API_KEY=
